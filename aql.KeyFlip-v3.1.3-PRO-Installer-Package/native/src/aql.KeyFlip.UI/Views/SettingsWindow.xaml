<Window x:Class="aql.KeyFlip.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - aql.KeyFlip"
        Height="460" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Settings" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   Margin="0,0,0,12"/>

        <!-- Tab Control -->
        <TabControl Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}">
            <!-- 1. General Tab -->
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12">
                    <StackPanel>
                        <CheckBox Name="StartWithWindowsCheckBox" Content="Start with Windows" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <CheckBox Name="StartMinimizedCheckBox" Content="Start minimized to tray" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <CheckBox Name="MinimizeToTrayCheckBox" Content="Minimize to Tray on close" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <CheckBox Name="NotificationsCheckBox" Content="Show notification on conversion" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <CheckBox Name="RestoreClipboardCheckBox" Content="Restore original clipboard content" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 2. Hotkey Tab -->
            <TabItem Header="Hotkey">
                <StackPanel Margin="12">
                    <TextBlock Text="Global Hotkey:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                    <DockPanel Margin="0,0,0,10">
                        <Button DockPanel.Dock="Right" 
                                Content="Save" 
                                Click="OnSaveHotkeyClick" 
                                Padding="12,4" 
                                Background="{StaticResource PrimarySkyBrush}" 
                                BorderBrush="{StaticResource PrimarySkyHoverBrush}" 
                                Foreground="White" 
                                FontWeight="SemiBold"/>
                        <TextBox Name="HotkeyTextBox" 
                                 Text="Ctrl + K" 
                                 FontFamily="Consolas" 
                                 FontWeight="Bold" 
                                 FontSize="12" 
                                 Padding="8,4" 
                                 Margin="0,0,8,0"
                                 Background="#1F2937" 
                                 Foreground="{StaticResource PrimarySkyBrush}" 
                                 BorderBrush="{StaticResource BorderBrush}"/>
                    </DockPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="Test Shortcut" Click="OnTestShortcutClick" Margin="0,0,8,0" Padding="8,4"/>
                        <Button Content="Reset Default" Click="OnResetDefaultHotkeyClick" Padding="8,4"/>
                    </StackPanel>

                    <TextBlock Name="HotkeyStatusTextBlock" 
                               Text="" 
                               FontSize="11" 
                               TextWrapping="Wrap" 
                               Foreground="{StaticResource SuccessGreenBrush}"/>
                </StackPanel>
            </TabItem>

            <!-- 3. Conversion Tab -->
            <TabItem Header="Conversion">
                <StackPanel Margin="12">
                    <TextBlock Text="Default Conversion Mode:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                    <RadioButton Name="ModeAutoRadio" Content="Auto (Intelligent Layout Detection)" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}" IsChecked="True"/>
                    <RadioButton Name="ModeEnToArRadio" Content="English → Arabic" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <RadioButton Name="ModeArToEnRadio" Content="Arabic → English" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </TabItem>

            <!-- 4. Appearance Tab -->
            <TabItem Header="Appearance">
                <StackPanel Margin="12">
                    <TextBlock Text="Theme:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                    <RadioButton Name="ThemeDarkRadio" Content="Dark" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}" IsChecked="True"/>
                    <RadioButton Name="ThemeLightRadio" Content="Light" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <RadioButton Name="ThemeSystemRadio" Content="System Default" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </TabItem>

            <!-- 5. Language Tab -->
            <TabItem Header="Language">
                <StackPanel Margin="12">
                    <TextBlock Text="User Interface Language:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                    <RadioButton Name="LangArRadio" Content="العربية (Arabic)" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}" IsChecked="True"/>
                    <RadioButton Name="LangEnRadio" Content="English" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <Button Content="Restore Defaults" 
                    Click="OnRestoreAllDefaultsClick" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Foreground="{StaticResource WarningAmberBrush}" 
                    FontSize="11"/>
            <Button DockPanel.Dock="Right" 
                    HorizontalAlignment="Right" 
                    Content="Done" 
                    Click="OnDoneClick" 
                    Padding="16,6" 
                    FontWeight="SemiBold"/>
        </DockPanel>
    </Grid>
</Window>
